# plugin-google-cloud-identity-account-collector
This is a plugin with the function of synchronizing accounts in Google Cloud.

docker run --rm -p 8080:8080 mitmproxy/mitmproxy mitmdump --mode regular